import React from 'react';

function SectionTitle({ title }) {
      return (
            <div>
                  <h1 style={{color: '--color-text-dark', textAlign:'center' }}>{title}</h1>

            </div>
            
      );
}

export default SectionTitle;